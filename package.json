{
  "name": "release-notes-generator",
  "private": true,
  "version": "0.2.0",
  "type": "commonjs",
  "dependencies": {
    "@actions/core": "^2.0.3",
    "@actions/github": "^6.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  },
  "scripts": {
    "build": "ncc build index.js -o dist"
  }
}