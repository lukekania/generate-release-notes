{
  "name": "release-notes-generator",
  "private": true,
  "version": "0.1.0",
  "type": "commonjs",
  "dependencies": {
    "@actions/core": "^2.0.2",
    "@actions/github": "^7.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  },
  "scripts": {
    "build": "ncc build index.js -o dist"
  }
}